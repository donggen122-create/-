# 컴퓨터에서 쓰는 가계부

브라우저에서 실행되는 간단한 가계부입니다.

## 기능
- 수입/지출 내역 추가
- 합계(총 수입/총 지출/잔액) 자동 계산
- 로컬 저장소(localStorage) 자동 저장
- 개별 삭제, 전체 삭제
- CSV 내보내기

## 실행 방법
1. `index.html` 파일을 브라우저로 열기
2. 또는 간단한 로컬 서버 실행:

```bash
python3 -m http.server 8000
```

브라우저에서 `http://localhost:8000` 접속
